# Installation

mockr requires Node.js v14 or higher.

## Use without installing

The easiest way — no install needed:

```bash
npx mockr-cli start
```

## Install globally

Install once and run from anywhere:

```bash
npm install -g mockr-cli
```

Once installed, run `mockr-cli start` from any folder.
